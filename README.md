# mcc-validate

Validator CLI for Model Context Certificates (MCC-STD-001).

## Installation

```bash
pip install mcc-validate
```

## Usage

```bash
mcc-validate check certificate.json
mcc-validate check certificate.json --format json
mcc-validate init --tier 3 --output new-cert.json
mcc-validate status certificate.json
```
